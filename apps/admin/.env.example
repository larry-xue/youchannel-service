VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=

# Local dev points the Vite admin UI at the jobs API. Omit this in production
# when the built admin UI is served by the jobs service on the same origin.
VITE_JOBS_API_URL=http://localhost:4000
